{
  "name": "artepaint",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm run dev",
    "start": "npm run dev",   // This will be used by Vercel
    "dev": "vite",
    "build": "npm run build", // This will be used by Vercel
    "server": "node server/server.js"  // This will be used by Heroku
    "all": "npm-run-all --parallel client server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
