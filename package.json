{
  "name": "artepaint",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm run dev",
    "start": "cd server && node server.js",
    "build": "cd client && npm install && npm run build && cd .. && npm install && npm run start",
    "all": "npm-run-all --parallel client server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
